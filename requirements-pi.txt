# Raspberry Pi specific dependencies
# These packages are only needed on the Pi and are installed by install.sh / update.sh.
# They are NOT needed for desktop/mock development.

# LCD display (HD44780 via I2C with PCF8574 backpack)
RPLCD>=1.3.1

# GPIO access (wire detection, hardware control)
RPi.GPIO>=0.7.1

# Higher-level GPIO API (wire detection via InputDevice)
gpiozero>=2.0

# I2C communication (LCD, PiSugar battery)
smbus2>=0.4.3

# USB input device reading (USB numpad via /dev/input/)
evdev>=1.6.0
