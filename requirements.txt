# Core dependencies (all platforms)
PyYAML>=6.0.1
Flask>=3.0.3
Werkzeug>=3.0.3

# Audio (all platforms — community edition, supports Python 3.11-3.13)
pygame-ce>=2.5.3

# Raspberry Pi specific (install only on Pi)
# Uncomment these when deploying to Raspberry Pi:
# RPLCD==1.3.1
# RPi.GPIO==0.7.1
# smbus2==0.4.3
